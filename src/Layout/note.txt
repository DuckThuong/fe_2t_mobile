2. Bài tập:
a. Tạo 4 màn của màn Forgot password
b. Link màn đầu tiên của màn forgot vào text "パスワードを忘れた場合" ở màn Login.
c. Truyền Email của màn đầu tiên của Forgot password sang màn thứ 2. Ở text "ログイン画面へ戻る" khi click phải
back về màn Login.
d. Ở màn thứ 2, hiển được Email đã truyền từ trang trước lên text "Eメール@gmail.com". Ở text "認証コードを再発行" 
khi click vào phải quay trở về được màn hình trước đó.
e. Ở màn thứ 3, email từ màn đầu tiên phải được fill vào input email. Input email phải set trạng thái là disable(email không 
được nhập tay từ ngoài vào), bắt lỗi mật khẩu mới và mật khẩu cũ phải giống nhau mới cho ấn button để next.
f. Button ở màn cuối cùng back về màn Login
